@model WebApplication2.Models.Plan

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Plan</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="InBirr" class="control-label"></label>
                <input asp-for="InBirr" class="form-control" />
                <span asp-validation-for="InBirr" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RevenuTypeId" class="control-label"></label>
                <select asp-for="RevenuTypeId" class ="form-control" asp-items="ViewBag.RevenuTypeId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
